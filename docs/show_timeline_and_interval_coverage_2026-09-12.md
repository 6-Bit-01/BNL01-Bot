# Show timeline and conversation coverage — September 12, 2026

## Problem and evidence

A named-track recall selected the correct September 11 show but rendered artist-biased dialogue from across the show. The capture's packet receipt reported zero visibility exclusions and zero budget exclusions. The selected dialogue was nevertheless limited to 950 characters by the show reader, then to 700 characters per item by the final renderer. Those limits could hide the actual conversation without appearing as packet exclusions.

The captured target interval has 26 matched original TikTok comments from six source identities. The delivered answer mentioned one thank-you and described light chat. That answer does not establish a useful account of the other conversation. The supplied selector output is a current reconstruction; it is not a saved historical provider prompt.

The existing show ledger already owns session operations, submissions, playback, wheel results, removals, track windows and TikTok records. Its Discord interaction view primarily represents exchanges with BNL. Ordinary public Discord chatter needs to be available independently of response pairing.

## Change

- Use the existing operational chronology to resolve a named track, last completed playback window, current observed track, elapsed show interval, or recorded wheel/sponsor interval. A title outranks other tracks by the same artist. An unresolved interval cannot silently become artist-wide recall.
- Read every retained human message in the resolved interval, irrespective of author, artist-name match, question status or whether BNL answered. Public Discord conversation rows and source-only public messages enter through the existing conversation/source owners, with duplicate original identities reconciled.
- Build one chronological read view containing operation records, TikTok comments, Discord comments and separately labeled BNL replies. BNL's words do not count as audience evidence or recurring human topics. A whole-show timeline includes retained pre-broadcast operations; its stated chat coverage remains the broadcast window.
- Use the same interval view for the authorized native live read model and the finalized show packet. A live observation ends at the accepted snapshot timestamp for the same live session. A source-selected show date narrows retained lookup instead of allowing a different archived show to answer a current-show question.
- Preserve the full selected interval through packet selection and rendering. The view has a 96,000-character bound. Dense views count all selected messages, retain whole utterances across the chronology and explicitly disclose omitted transcript entries. They cannot claim complete coverage or infer silence from omissions.
- Retain the first-party operations and revisable community source classes for whole-show requests. Replace their shortened dialogue view with the chronological conversation. Existing quote, image, multi-show and person-recall owners keep their existing contracts.

This extends the existing show/source/packet owners. It adds no memory store, answer judge, provider call, queue command, production gate change or automatic historical rewrite. Accepted room humor remains part of the attributed conversation.

## Verification

Twenty focused tests exercise named/relative/current windows, title disambiguation, half-open boundaries, all 26 neutral fixture messages through the actual database and default packet renderer, missing/oversized source disclosure, public Discord messages without a BNL reply, source-only messages, source revalidation, owner labeling, live snapshot timing and whole-show chronology. The 18 existing show-ledger tests also pass, including original source authority, continuity and source revocation checks. The required `make check` completed with exit code 0; independent discovery confirms 3,033 tests in that suite. Its captured successful log omits the final textual summary, so the working-pack receipt records the exit status and count basis explicitly. The PR records the exact tested tree.

A separate private working-pack replay preserves all 26 supplied original comments and six source identities in the interval view. Its start is reconstructed from the captured duration and roster end. It is a bounded view replay, not a full production database or live provider replay. Real captured transcripts are not included in this repository change.

## Acceptance still to perform

After the reviewed candidate is merged and deployed, use the existing September 11 named-track question in bnl-testing. Compare the resulting source window, message coverage, source digests, final packet and delivered answer. A useful answer should recognize the wider discussion and distinguish explicit song reactions from coincident room banter. It need not recite every comment.

Then ask for that show's timeline and chat rundown. Verify operation order against original session records and verify TikTok/Discord coverage separately. During a genuine later live show, check current-track chat after the latest queue operation, track transitions and a recorded wheel interval. Mark these live checks open until their evidence is supplied.

The older captured show projection is not a full export of all 258 operation rows or 1,158 TikTok rows. Upstream capture gaps, earlier text shortening, pre-broadcast chat, semantics of arbitrary named Moments, and unseen events remain outside claims of completeness. Narrower interval reads remain available when a whole-show transcript exceeds the explicit bound.

The deployed Moment meaning repair (#542) still awaits a genuine new eligible Moment. The separately captured dropped-correction case remains open for its own existing conversation/context-path repair. Preserve the original 11 requirements, six stages, 17 owners and already earned passes.
