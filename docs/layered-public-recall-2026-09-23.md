# Layered public recall — September 23 acceptance repair

The deployed PR577 sequence exposed conflicts after individual readers had
passed their local checks. The next repair keeps the locked shared-brain
completion plan and its existing owners. Public information is layered into
one understanding of the turn; a later projection cannot independently change
the person/topic or prohibit quoting an eligible original supplied earlier.

## Evidence and diagnosis

The September 23 production capture, PID 1753205, establishes:

- Recent named Discord originals reached the model. The follow-up also loaded
  the same seven originals, yet the answer demanded an audit block and gave
  paraphrases. The original text was available; exact cause inside the model
  is unobservable. The assembled contracts contained blanket quotation limits
  that did not clearly distinguish derived examples from original messages.
- The explicit May 8 person/date request was classified as ambiguous because
  “Quote it” was resolved against several preceding contributions. No named
  original/tier read ran for that request.
- The cross-platform member request selected Discord messages about TikTok
  (`selection=topic query_terms=3`) and loaded no show evidence. Platform and
  request words were being treated as required message topics.
- The self-activity question did not bind the current speaker. Its plausible
  answer is not accepted as evidence of correct personal source scope.
- The Chris switch quoted a matching public original, and the memory question
  delivered short, medium, long and relationship context. Those reads do not
  establish aging, consolidation quality, learned identity, or Core health.

A fictional replay with preceding human and model messages reproduces the
selection failures. Integration testing also exposed the later packet selector
re-expanding a resolved topic follow-up to newer unrelated show material.

## Existing owners changed

1. Context v2 retains the current complete, resolved person/date lookup. An
   explicit Discord reply and an unresolved member identity retain their
   existing semantics. “Quote it” inside that lookup is not an instruction to
   choose among previous bot answers.
2. Discord originals, named tiers and show participant recall share request
   vocabulary. Platform names select evidence sources; they do not require the
   original comment to contain those platform names. Actual content topics
   still rank and filter relevant messages before bounds.
3. The frame and public readers share explicit self-activity recognition.
   Missing requester evidence cannot expand to somebody else's activity.
   Public owner self-recall renders 6 Bit; private account labels and private
   channel records stay outside the public projection.
4. The packet projection carries the native reader's resolved human selection
   query. Refresh uses that same query. Current explicit dates remain primary.
   The current request itself is unchanged, and the source basis continues to
   invalidate on deleted/edited/private/retracted original evidence.
5. One ordinary recall contract distinguishes original quotation from derived
   interpretation. Original messages supply wording and attribution; tiers,
   relationships and Moments add history and meaning. Their summaries cannot
   invent wording, override originals or establish canon. Consequential
   current-room dispute verification still requires its existing live source
   authority. Voice and established canon cannot invent events or identities.

No identity aliases are seeded, no source data is migrated, and no gate or
provider route is enabled by this repair.

## Verification and limits

The regression sequence runs direct assembly and the final batched provider
prompt with the ordinary packet on/off: broad member recall, exact words,
member switch, older dated quote, named memory, self activity, and combined
Discord/TikTok recall. It checks original source rows, show excerpts, older
memory, quotation instructions, subject/date scope, and one delivered reply.
Existing topic-continuation, source invalidation, privacy and consequential
quote tests remain required. A separate self-activity test verifies both
platforms; the owner projection test verifies safe labels and privacy changes.

Model and Discord transports are fixtures. These tests prove evidence and
contract composition and delivery controls; they cannot certify live model
interpretation. Deployed acceptance of the failed turns remains required.
Earlier verified show/quote cases remain credited. Learned identity,
correction/retraction of identity, natural memory meaning/aging/reopening,
fresh queue/live evidence, and natural Journal/Relay/Ballad/Ambient delivery
remain the locked plan's open acceptance conditions.
